pwd: To check in which folder we are
cd .. : To go out of folder
cd folder name : To go in that particular folder
git Status : To check status.(displays the state of the working directory and the staging area.)
git add -A : To add all the files. (adds a change in the working directory to the staging area.)
git commit : To add the files in GIT(To create a snapshot of the staged changes along
a timeline of a Git projects history.)
git commit -m 'Message': To add the files in GIT and give the message.
git push : To add the work to the Github.(To upload local repository content to a remote repository.)
git pull : To fetch and download content from a remote repository
and immediately update the local repository to match that content.